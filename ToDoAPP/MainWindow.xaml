<Window x:Class="ToDoAPP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoAPP"
        mc:Ignorable="d"
        Title="Simple To-Do List" Height="600" Width="800">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          Background="{TemplateBinding Background}" 
                                          BorderThickness="{TemplateBinding BorderThickness}" 
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                                            <Path x:Name="Arrow" Fill="{DynamicResource TextBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="10,3,30,3" VerticalAlignment="Center" HorizontalAlignment="Left"/>

                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="{DynamicResource PopupBackgroundBrush}" BorderThickness="1" BorderBrush="Gray"/>
                                    <ScrollViewer Margin="0,2,0,2" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="ActiveTaskTemplate">
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <CheckBox Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0" Checked="TaskCompleted_Checked" Tag="{Binding}" />
                <TextBlock Grid.Column="1" Text="{Binding TaskNumber, StringFormat='{}{0}.'}" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold" />
                <Border Grid.Column="2" Background="{Binding PriorityColor}" Width="12" Height="12" CornerRadius="6" Margin="0,0,10,0" ToolTip="{Binding PriorityName}" />
                <TextBlock Grid.Column="3" Text="{Binding Description}" VerticalAlignment="Center" TextWrapping="Wrap" />
                <TextBlock Grid.Column="4" Text="{Binding DueDate, StringFormat='{}{0:d}'}" VerticalAlignment="Center" Margin="10,0,10,0" FontStyle="Italic" Foreground="Gray" />

                <StackPanel Grid.Column="5" Orientation="Horizontal" Margin="10,0,0,0">
                    <Button Click="EditTask_Click" Padding="5" ToolTip="Edit Task" Margin="0,0,5,0" Tag="{Binding}">
                        <TextBlock Text="✏️" FontFamily="Segoe UI Emoji" />
                    </Button>
                    <Button Click="DeleteTask_Click" Padding="5" ToolTip="Delete Task" Tag="{Binding}">
                        <TextBlock Text="🗑️" FontFamily="Segoe UI Emoji" />
                    </Button>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="CompletedTaskTemplate">
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <CheckBox Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0" IsChecked="True" Unchecked="TaskCompleted_Unchecked" Tag="{Binding}" />
                <TextBlock Grid.Column="1" Text="{Binding TaskNumber, StringFormat='{}{0}.'}" VerticalAlignment="Center" Margin="0,0,10,0" TextDecorations="Strikethrough" Foreground="Gray" />
                <Border Grid.Column="2" Background="{Binding PriorityColor}" Width="12" Height="12" CornerRadius="6" Margin="0,0,10,0" ToolTip="{Binding PriorityName}" Opacity="0.5" />
                <TextBlock Grid.Column="3" Text="{Binding Description}" VerticalAlignment="Center" TextWrapping="Wrap" TextDecorations="Strikethrough" Foreground="Gray" />
                <TextBlock Grid.Column="4" Text="{Binding DueDate, StringFormat='{}{0:d}'}" VerticalAlignment="Center" Margin="10,0,10,0" FontStyle="Italic" TextDecorations="Strikethrough" Foreground="Gray" />

                <StackPanel Grid.Column="5" Orientation="Horizontal" Margin="10,0,0,0">
                    <Button Click="DeleteTask_Click" Padding="5" ToolTip="Delete Task" Tag="{Binding}">
                        <TextBlock Text="🗑️" FontFamily="Segoe UI Emoji" />
                    </Button>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="New Task" Width="100" Padding="5" Click="ShowTaskPopup_Click" />
                <Button Content="Filter" Width="100" Margin="10,0,0,0" Padding="5" Click="ShowFilterPopup_Click" />
                <Button Content="Settings" Width="100" Margin="10,0,0,0" Padding="5" Click="ShowSettingsPopup_Click" />
            </StackPanel>

            <ListBox x:Name="TaskListView" Grid.Row="1" FontSize="16" HorizontalContentAlignment="Stretch" ItemTemplate="{StaticResource ActiveTaskTemplate}" />

            <TextBlock Grid.Row="2" Text="Completed Tasks" FontSize="18" FontWeight="Bold" Margin="0,10,0,10" Foreground="{DynamicResource TextBrush}" />

            <ListBox x:Name="CompletedTaskListView" Grid.Row="3" FontSize="16" HorizontalContentAlignment="Stretch" ItemTemplate="{StaticResource CompletedTaskTemplate}" />
        </Grid>

        <Grid x:Name="TaskPopup" Visibility="Collapsed" Background="#80000000">
            <Border Background="{DynamicResource PopupBackgroundBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20" CornerRadius="5" MinWidth="350">
                <StackPanel>
                    <TextBlock x:Name="PopupTitle" Text="Create Task" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource TextBrush}" />

                    <TextBlock Text="Task Description:" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}" />
                    <TextBox x:Name="TaskInput" Height="30" Margin="0,0,0,15" Padding="5" KeyDown="TaskInput_KeyDown" />

                    <TextBlock Text="Due Date:" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}" />
                    <DatePicker x:Name="TaskDatePicker" Margin="0,0,0,15" />

                    <TextBlock Text="Priority:" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}" />
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="PriorityComboBox" Grid.Column="0" Height="30">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="{Binding Color}" Width="12" Height="12" CornerRadius="6" Margin="0,0,5,0" />
                                        <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextBrush}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <Button Grid.Column="1" Content="New" Padding="5,0" Margin="10,0,0,0" Height="30" Click="AddPriority_Click" />
                        <Button Grid.Column="2" Content="Edit" Padding="5,0" Margin="5,0,0,0" Height="30" Click="EditPriority_Click" />
                        <Button Grid.Column="3" Content="Del" Padding="5,0" Margin="5,0,0,0" Height="30" Click="DeletePriority_Click" />
                    </Grid>

                    <Border x:Name="PriorityEditorPanel" Visibility="Collapsed" Margin="0,0,0,15" Background="#80000000" Padding="10" CornerRadius="5">
                        <StackPanel>
                            <TextBlock Text="Priority Name:" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}" />
                            <TextBox x:Name="PriorityNameInput" Height="30" Padding="5" Margin="0,0,0,10" />

                            <TextBlock Text="Priority Color:" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}" />
                            <ComboBox x:Name="PriorityColorComboBox" Height="30" Margin="0,0,0,10">
                                <ComboBoxItem Content="Red" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBoxItem Content="Blue" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBoxItem Content="Green" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBoxItem Content="Yellow" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBoxItem Content="Orange" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBoxItem Content="Purple" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBoxItem Content="Pink" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBoxItem Content="Gray" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBoxItem Content="Cyan" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBoxItem Content="Brown" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBoxItem Content="Magenta" Foreground="{DynamicResource TextBrush}"/>
                                <ComboBoxItem Content="Goldenrod" Foreground="{DynamicResource TextBrush}"/>
                            </ComboBox>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Cancel" Width="60" Margin="0,0,10,0" Padding="5" Click="CancelPriorityEdit_Click" />
                                <Button Content="Save" Width="60" Padding="5" Click="SavePriority_Click" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Width="80" Margin="0,0,10,0" Padding="5" Click="CloseTaskPopup_Click" />
                        <Button x:Name="AddButton" Content="Save Task" Width="80" Padding="5" Click="AddTask_Click" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <Grid x:Name="FilterPopup" Visibility="Collapsed" Background="#80000000">
            <Border Background="{DynamicResource PopupBackgroundBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20" CornerRadius="5" MinWidth="350">
                <StackPanel>
                    <TextBlock Text="Filter &amp; Sort Tasks" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource TextBrush}" />

                    <TextBlock Text="Priority:" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}" />
                    <ComboBox x:Name="FilterPriorityComboBox" Height="30" Margin="0,0,0,15">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Border Background="{Binding Color}" Width="12" Height="12" CornerRadius="6" Margin="0,0,5,0" />
                                    <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextBrush}" />
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <TextBlock Text="Due Date:" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}" />
                    <DatePicker x:Name="FilterDatePicker" Height="30" Margin="0,0,0,15" />

                    <TextBlock Text="Sort By:" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}" />
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="SortByComboBox" Grid.Column="0" Margin="0,0,5,0" Height="30">
                            <ComboBoxItem Content="None" IsSelected="True" Foreground="{DynamicResource TextBrush}"/>
                            <ComboBoxItem Content="Due Date" Foreground="{DynamicResource TextBrush}"/>
                            <ComboBoxItem Content="Priority" Foreground="{DynamicResource TextBrush}"/>
                        </ComboBox>
                        <ComboBox x:Name="SortDirectionComboBox" Grid.Column="1" Margin="5,0,0,0" Height="30">
                            <ComboBoxItem Content="Ascending / Latest" IsSelected="True" Foreground="{DynamicResource TextBrush}"/>
                            <ComboBoxItem Content="Descending / Last" Foreground="{DynamicResource TextBrush}"/>
                        </ComboBox>
                    </Grid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Clear Filter" Width="90" Margin="0,0,10,0" Padding="5" Click="ClearFilter_Click" />
                        <Button Content="Cancel" Width="80" Margin="0,0,10,0" Padding="5" Click="CloseFilterPopup_Click" />
                        <Button Content="Apply" Width="80" Padding="5" Click="ApplyFilter_Click" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <Grid x:Name="SettingsPopup" Visibility="Collapsed" Background="#80000000">
            <Border Background="{DynamicResource PopupBackgroundBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20" CornerRadius="5" MinWidth="250">
                <StackPanel>
                    <TextBlock Text="Settings" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource TextBrush}" />

                    <TextBlock Text="Theme:" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}" />
                    <ComboBox x:Name="ThemeComboBox" Margin="0,0,0,20" Height="30" SelectionChanged="ThemeComboBox_SelectionChanged">
                        <ComboBoxItem Content="Light" Foreground="{DynamicResource TextBrush}" />
                        <ComboBoxItem Content="Dark" Foreground="{DynamicResource TextBrush}" />
                    </ComboBox>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Close" Width="80" Padding="5" Click="CloseSettingsPopup_Click" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>