<Window x:Class="ToDoAPP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoAPP"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="TaskInput" Width="300" Margin="0,0,10,0" Padding="5" />
            <Button x:Name="AddButton" Content="Add Task" Width="100" Click="AddTask_Click" />
        </StackPanel>

        <ListBox x:Name="TaskListView" Grid.Row="1" FontSize="16" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding}" VerticalAlignment="Center" TextWrapping="Wrap" />
                        <!-- Edit Button with an Icon -->
                        <Button Grid.Column="1" Click="EditTask_Click" Margin="10,0,0,0" Padding="5" ToolTip="Edit Task"
                                Tag="{Binding}">
                            <TextBlock Text="✏️" FontFamily="Segoe UI Emoji" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
